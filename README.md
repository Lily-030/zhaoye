# Ye Zhao Portfolio

赵烨的个人作品集与简历网站，采用单文件 HTML、CSS 和原生 JavaScript 构建。

主页为 `index.html`，个人图片和视频放在 `assets/`。详细文件名与上传方法见 `assets/README.md`。

## GitHub Pages

在仓库 **Settings → Pages** 中，将 **Source** 设置为 **Deploy from a branch**，选择 `main` 分支和 `/ (root)` 目录并保存。
